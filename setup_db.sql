-- データベースの作成
CREATE DATABASE IF NOT EXISTS study_game;
USE study_game;

-- ユーザーの作成（必要に応じて）
-- CREATE USER 'study_user'@'localhost' IDENTIFIED BY 'study_password';
-- GRANT ALL PRIVILEGES ON study_game.* TO 'study_user'@'localhost';
-- FLUSH PRIVILEGES;
